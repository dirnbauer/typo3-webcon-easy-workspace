{
  "name": "webcon-easy-workspace",
  "private": true,
  "description": "JavaScript unit tests for the Easy Workspace toolbar (runtime: TYPO3 import map, no build step).",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "jsdom": "^30.1.1",
    "lit": "^3.3.3",
    "vitest": "^5.0.1"
  },
  "engines": {
    "node": ">=22.22.2"
  }
}
